<%= form_with(model: proforma, local: true) do |form| %>
  <% if proforma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proforma.errors.count, "error") %> prohibited this proforma from being saved:</h2>

      <ul>
        <% proforma.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cod_proforma %>
    <%= form.number_field :cod_proforma %>
  </div>

  <div class="field">
    <%= form.label :cod_solictud %>
    <%= form.number_field :cod_solictud %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="field">
    <%= form.label :usu_crea %>
    <%= form.text_field :usu_crea %>
  </div>

  <div class="field">
    <%= form.label :fec_crea %>
    <%= form.datetime_select :fec_crea %>
  </div>

  <div class="field">
    <%= form.label :usu_mod %>
    <%= form.text_field :usu_mod %>
  </div>

  <div class="field">
    <%= form.label :fec_mod %>
    <%= form.datetime_select :fec_mod %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
